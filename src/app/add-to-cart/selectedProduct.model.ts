export interface selectedProduct {
    name: string;
    description: string;
    price: string;
    image: Array<ArrayBuffer>;
    id: string;
    featuredImage: string;
    sizes: Array<ArrayBuffer>;
    selectedSize: string;
}